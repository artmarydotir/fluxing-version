// this is for global mutations that do not group by special category or modules
// dont forget to import in store.js


export const anotherMutations = () => {};

export const updatedVal = (state, payload) => {
  state.value = payload;
};
